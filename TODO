Bugs
  - Show color no worky when small

features for v1.0.0
  - preflight_check (run only once per compile)
  - builtin function to display all user-mapped commands
  - builtin function to quickedit pshd file using $EDITOR
  - make install

features for v1.1+
  - optional single menu mode [launch into a menu]
  - full command list option
  - ability to pass args to commands

features for v2.0.0
  - remove END_OF_MENU requirement by implementing custom vector type

Bugs Fixed since last release
  - Prevent artifacts of parent menus from showing in submenus of smaller size
  - shutter_slide isn't using fg_c
  - hd[] hd_offset_y was repositioning hd in the wrong direction
